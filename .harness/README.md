# Harness Directory

This directory contains the operating system for Codex-assisted development.

- `rules/`: durable engineering rules
- `templates/`: task, plan, verification, review templates
- `skills/`: reusable Codex workflows
- `runs/`: per-task execution records

Do not delete run records for merged work. They are used for retrospectives and process improvement.
